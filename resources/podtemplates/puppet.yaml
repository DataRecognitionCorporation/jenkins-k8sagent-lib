spec:
  containers:
  - name: puppet
    image: 177429746880.dkr.ecr.us-east-1.amazonaws.com/jenkins/agent/puppet:latest
    resources:
      limits:
        cpu: 2000m
        memory: 4Gi
      requests:
        cpu: 1500m
        memory: 2Gi
    # tty: true
    securityContext:
      privileged: true
    idleMinutes: 5
  # hostNetwork: false
  name: puppet
  label: puppet 
  # coel7_agent_aws aws role_prod
