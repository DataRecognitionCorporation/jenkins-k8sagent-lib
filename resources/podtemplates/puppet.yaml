spec:
  containers:
  - name: puppet
    image: 177429746880.dkr.ecr.us-east-1.amazonaws.com/jenkins/agent/puppet:latest
    resources:
      limits:
        cpu: 1000m
        memory: 4Gi
      requests:
        cpu: 500m
        memory: 4Gi
    # tty: true
    securityContext:
      privileged: true
  # hostNetwork: false
  label: puppet
  name: puppet